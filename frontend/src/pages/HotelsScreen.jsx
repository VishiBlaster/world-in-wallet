import React from 'react';
import Hero from '../components/Header/Hero';

const HotelsScreen = () => {
	return (
		<div>
			<Hero id='hotels' title='Find your place to stay' />
		</div>
	);
};

export default HotelsScreen;
